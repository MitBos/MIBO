@* Summary: Defines an import map so the reconnect modal script can be resolved via a stable module path. *@
<script type="importmap">
{
  "imports": {
    "mibo/reconnect-modal": "/Components/Layout/ReconnectModal.razor.js"
  }
}
</script>
